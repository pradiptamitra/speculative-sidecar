# Tested with Python 3.12. Install into a fresh venv, e.g.:
#   uv venv --python 3.12 .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# On Mac, also: export PYTORCH_ENABLE_MPS_FALLBACK=1
torch
transformers
accelerate
datasets
